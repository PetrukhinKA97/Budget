<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="View.Budget.VExpenses_add_chan">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Название"/>
            <Entry Text="{Binding Name}"/>
            <Label Text="Дата"/>
            <DatePicker Date="{Binding Data}"/>
            <Label Text="Сумма"/>
            <Entry Text="{Binding Amount}"/>
            <Label Text="Количество"/>
            <Entry Text="{Binding Number}"/>
            <Label Text="Обьем"/>
            <Entry Text="{Binding Volume}"/>
            <Label Text="Тип трат"/>
            <Picker BindingContext="{Binding Type_expenses}"/>
            <Label Text="Кретичность"/>
            <Picker BindingContext="{Binding Importance}" />
            <Button Text="Добавить траты" Clicked="Importance_add_Clicked"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>